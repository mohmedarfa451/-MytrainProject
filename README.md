# MyFirstProject

This is my first project with Node.js backend and HTML/CSS/JS frontend.

## How to run

1. Install dependencies: `npm install`
2. Run the server: `node server.js`
3. Open browser at: `http://localhost:3000`
